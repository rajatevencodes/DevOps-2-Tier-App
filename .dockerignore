# Docker-related files
# These are used to build and run the container, but are not needed inside it.
Dockerfile
docker-compose.yaml
.dockerignore
kubernetes/

# Git-related files
# Your Git history and configuration should not be part of the image.
.git
.gitignore

# Documentation
# Markdown files are for developers, not for the application runtime.
*.md
README.md
RunLocally.md

# Local development scripts
# These scripts are for managing the local environment, not the container.
*.sh
startLocalServer.sh
stopLocalServer.sh

# CI/CD pipeline files
# Jenkinsfile is for your build server, not for the application.
Jenkinsfile

# Python-specific files
# These are generated by Python or package managers and can be safely ignored.
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
*.egg-info/
dist/
build/

# IDE/Editor configuration files
# To keep the image clean from local development environment settings.
.vscode/
.idea/
*.swp

# Database files
# SQL files are often used for initialization but might not be needed in the final image
# unless your application reads from it directly at runtime.
*.sql
message.sql

